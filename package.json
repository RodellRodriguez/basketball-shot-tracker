{
    "name": "basketball-shot-tracker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "build": "pnpm run -r build",
      "dev": "pnpm run -r dev",
      "test": "pnpm run -r test",
      "lint": "pnpm run -r lint"
    },
    "workspaces": [
    "packages/*"
  ],
    "devDependencies": {
      "@types/node": "~20",
      "typescript": "^4.2.2"
    }
  }